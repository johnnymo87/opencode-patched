# v1.4.0 Patch Refresh Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Refresh the OpenCode patch stack for upstream `v1.4.0`, dropping or rebasing patches based on current upstream behavior and correct repo ownership.

**Architecture:** Use fresh upstream `v1.4.0` checkouts as the implementation base. Refresh `tool-fix` and `vim` in `opencode-patched`, refresh `caching` in `opencode-cached`, then verify the full stack by applying the owned patches in order and running typecheck plus targeted regression tests.

**Tech Stack:** Git patches, Bun, TypeScript, OpenCode monorepo, GitHub PR diffs, local sibling repos in `~/projects`

---

### Task 1: Verify Whether Tool Fix Still Needs a Patch

**Files:**
- Inspect: `~/projects/opencode-patched/patches/tool-fix.patch`
- Inspect: upstream `v1.4.0` checkout `packages/opencode/src/session/message-v2.ts`
- Inspect: upstream `v1.4.0` checkout `packages/opencode/test/session/message-v2.test.ts`

**Step 1: Write the failing test**

Use the upstream PR `#16751` regression test shape as the expected behavior if the test is not already present upstream.

**Step 2: Run test to verify it fails or determine it already passes**

Run against a plain `v1.4.0` checkout:

```bash
bun --cwd packages/opencode test test/session/message-v2.test.ts
```

Expected:
- If behavior is missing, the targeted regression fails.
- If behavior already exists upstream, the regression already passes and the patch can likely be dropped.

**Step 3: Write minimal implementation**

- If upstream already has the behavior, remove `patches/tool-fix.patch` and update docs accordingly.
- If upstream lacks the behavior, refresh `patches/tool-fix.patch` from the current `#16751` behavior with the smallest possible diff.

**Step 4: Run test to verify it passes**

Run:

```bash
bun --cwd packages/opencode test test/session/message-v2.test.ts
```

Expected: PASS for the targeted regression.

**Step 5: Commit**

Do not commit unless explicitly requested.

### Task 2: Rebase Vim Behavior onto v1.4.0

**Files:**
- Modify: `~/projects/opencode-patched/patches/vim.patch`
- Inspect: PR guide `/tmp/vim-pr-12679.patch` or refreshed `gh pr diff 12679`
- Inspect in upstream checkout:
  - `packages/opencode/src/cli/cmd/tui/app.tsx`
  - `packages/opencode/src/cli/cmd/tui/component/prompt/index.tsx`
  - `packages/opencode/src/config/tui-schema.ts`
  - any additional touched vim-related files under `packages/opencode/src/cli/cmd/tui/component/vim/*`
  - docs files if needed by upstream structure

**Step 1: Write the failing test**

Add or identify the smallest targeted test coverage available for vim-mode behavior. If no automated prompt/TUI test exists, use typecheck plus a narrower unit-level assertion around the new helper logic where feasible.

**Step 2: Run test to verify it fails**

Run the most targeted available verification first, then package typecheck:

```bash
bun --cwd packages/opencode typecheck
```

Expected: failure before the rebase if new code or tests expose missing behavior, or clean baseline before porting helper code.

**Step 3: Write minimal implementation**

- Re-port the PR `#12679` behavior onto `v1.4.0`.
- Use the PR as the behavioral source of truth.
- Keep only behavior still missing upstream.
- Regenerate `patches/vim.patch` from the final diff.

**Step 4: Run test to verify it passes**

Run:

```bash
bun --cwd packages/opencode typecheck
```

Plus any targeted tests added in Step 1.

Expected: PASS.

**Step 5: Commit**

Do not commit unless explicitly requested.

### Task 3: Rebase Caching Patch in opencode-cached

**Files:**
- Modify: `~/projects/opencode-cached/patches/caching.patch`
- Inspect in upstream checkout:
  - `packages/opencode/src/provider/transform.ts`
  - `packages/opencode/src/session/prompt.ts`
  - `packages/opencode/src/config/config.ts`
  - `packages/opencode/src/provider/config.ts`
  - related tests under `packages/opencode/test/provider/*`

**Step 1: Write the failing test**

Repair or add the smallest targeted provider/prompt regression tests needed for the explicit caching behavior on `v1.4.0`.

**Step 2: Run test to verify it fails**

Run targeted provider tests and package typecheck in a plain `v1.4.0` checkout after introducing the regression coverage.

**Step 3: Write minimal implementation**

- Rebase the caching behavior in `~/projects/opencode-cached`.
- Prefer adapting to current provider/prompt structure over preserving old hunk layout.
- Fix stale test typing issues while keeping the implementation minimal.

**Step 4: Run test to verify it passes**

Run targeted tests and:

```bash
bun --cwd packages/opencode typecheck
```

Expected: PASS.

**Step 5: Commit**

Do not commit unless explicitly requested.

### Task 4: Update Refresh Documentation in opencode-patched

**Files:**
- Modify: `~/projects/opencode-patched/README.md`
- Create: `~/projects/opencode-patched/.opencode/skills/patch-refresh.md`

**Step 1: Write the failing test**

Use a documentation checklist instead of executable tests:

- README states patch ownership by repo.
- README explains that vim uses PR `#12679` as a behavioral guide.
- README explains that tool-fix may be dropped if upstream includes it.
- `.opencode/skills/patch-refresh.md` explains when to edit sibling repos in `~/projects`.

**Step 2: Run checklist to verify it fails**

Inspect current files and confirm the ownership model is not yet documented completely.

**Step 3: Write minimal implementation**

- Update `README.md` maintenance sections.
- Add `.opencode/skills/patch-refresh.md` with concise repo-specific guidance.

**Step 4: Run checklist to verify it passes**

Re-read both files and confirm each checklist item is present.

**Step 5: Commit**

Do not commit unless explicitly requested.

### Task 5: Verify the Full v1.4.0 Patch Stack

**Files:**
- Verify in a fresh upstream `v1.4.0` checkout using:
  - `~/projects/opencode-cached/patches/caching.patch`
  - `~/projects/opencode-patched/patches/vim.patch`
  - `~/projects/opencode-patched/patches/tool-fix.patch` if still needed

**Step 1: Write the failing test**

The stack-level test is a clean application and verification run from fresh upstream.

**Step 2: Run test to verify it fails**

Before fixes, capture which patches fail `git apply --check` or which verification commands fail.

**Step 3: Write minimal implementation**

Use the refreshed patch files from Tasks 1-4.

**Step 4: Run test to verify it passes**

Run:

```bash
git apply /path/to/caching.patch
git apply /path/to/vim.patch
git apply /path/to/tool-fix.patch
bun install --frozen-lockfile
bun --cwd packages/opencode typecheck
```

Also run targeted regression tests for tool-fix and caching if those patches remain.

Expected: all owned patches apply cleanly and verification passes.

**Step 5: Commit**

Do not commit unless explicitly requested.
