VERDICT: APPROVE WITH CHANGES

VERIFIED CLAIMS:

1. CONFIRMED - `87c33b3` is an ancestor of `v1.17.7`. Evidence: `git -C "/home/dev/projects/opencode" merge-base --is-ancestor 87c33b3 v1.17.7 && printf 'ANCESTOR: yes\n' || printf 'ANCESTOR: no exit=%s\n' "$?"` returned `ANCESTOR: yes`.

2. CONFIRMED - `v1.17.7` still uses `Layer.makeMemoMapUnsafe()` in `server.ts`. Evidence: `git -C "/home/dev/projects/opencode" grep -n "Layer.makeMemoMapUnsafe" v1.17.7 -- packages/opencode/src/server/server.ts` returned `v1.17.7:packages/opencode/src/server/server.ts:125:  return Layer.buildWithMemoMap(listenerLayer(opts, port), Layer.makeMemoMapUnsafe(), scope).pipe(`.

3. CONFIRMED - the upstream regression test exists in `v1.17.7`. Evidence: `git -C "/home/dev/projects/opencode" grep -n "plugin client requests reuse the listening server instance" v1.17.7 -- packages/opencode/test/server/httpapi-listen.test.ts` returned `v1.17.7:packages/opencode/test/server/httpapi-listen.test.ts:305:  test("plugin client requests reuse the listening server instance", async () => {`. The test creates a plugin that calls `input.client.config.get()`, starts a listener, fetches `/config` with `x-opencode-directory`, waits for the plugin client call to complete, and asserts the plugin initialized once.

4. CONFIRMED, scoped to the known `opencode serve` failure mode - `87c33b3` is a narrow plugin bridge, not an architectural memoMap fix. Evidence: `git -C "/home/dev/projects/opencode" show --name-status --oneline 87c33b3` returned changes to `packages/opencode/src/plugin/index.ts`, `packages/opencode/src/server/server.ts`, and `packages/opencode/test/server/httpapi-listen.test.ts`. The diff changes plugin client creation to use `Server.url` as `baseUrl` and only install `fetch: Server.Default().app.fetch` when no listener URL exists; `server.ts` makes `url` optional and clears it on listener stop. Other direct in-process consumers still exist: `git -C "/home/dev/projects/opencode" grep -n "Server.Default().app.fetch" v1.17.7 -- packages/opencode/src` returned `cli/cmd/run.ts:846`, `cli/cmd/run.ts:884`, `cli/tui/worker.ts:35`, and `plugin/index.ts:146`. For normal `opencode serve`, the reasoning is sound: `serve.ts:19` starts `Server.listen`, external clients use real HTTP, and plugin SDK calls now reuse that listener. Residual paths remain: local `opencode run` modes still use in-process `Default` without a listener, and `cli/tui/worker.ts` can expose RPC `fetch` while also being able to start a listener, although current external TUI mode uses the listener URL with `fetch: undefined` (`cli/cmd/tui.ts:157-166`). The dropped patch would have covered future or atypical same-process mixed `Default`+listener consumers; upstream only covers the observed plugin-triggered `serve` scenario.

5. CONFIRMED - `main` is stale and diverged, and `merge -X theirs release/v1.17` does not guarantee an exact `release/v1.17` tree. Evidence: `git -C "/home/dev/projects/opencode-patched-v1.17.7-cutover" merge-base main release/v1.17` returned `c122c580a64bc0881defcf624212ad2046097850`, and `git -C "/home/dev/projects/opencode-patched-v1.17.7-cutover" rev-list --left-right --count main...release/v1.17` returned `4	6`. Evidence for staleness: `git -C "/home/dev/projects/opencode-patched-v1.17.7-cutover" grep -n "TARGET UPSTREAM\|prompt-loop-cache\|instance-state-partition" main -- patches/apply.sh README.md .github/workflows/build-release.yml` showed `main:patches/apply.sh:20:# TARGET UPSTREAM: opencode v1.16.2` plus active `prompt-loop-cache` references. Evidence for main-only stale files: `git -C "/home/dev/projects/opencode-patched-v1.17.7-cutover" diff --name-status release/v1.17 main -- patches README.md .github/workflows` returned `A patches/cache-aligned-compaction.patch`, `A patches/mcp-reconnect.patch`, and `A patches/prompt-loop-cache.patch`, among other diffs. Evidence for CI dependency on main: `sync-upstream.yml:77-80` triggers `build-release.yml --ref main`. The plan's empty-diff guard is the right safety gate, but the merge mechanics need to be made exact; see Blocking Issue 1.

BLOCKING ISSUES:

1. Phase 4.2 must not rely on `git merge -X theirs release/v1.17` plus ad hoc deletion. `-X theirs` only resolves conflicting hunks; it preserves main-only files and can preserve non-conflicting main-side edits. Exact fix: replace the preferred command sequence with an exact-tree merge commit, or explicitly restore every path from `release/v1.17` before committing. A safe no-force sequence is:

```bash
git -C ~/projects/opencode-patched checkout main
git -C ~/projects/opencode-patched merge --no-ff --no-commit -s ours release/v1.17
git -C ~/projects/opencode-patched restore --source=release/v1.17 --staged --worktree :/
git -C ~/projects/opencode-patched diff --cached --exit-code release/v1.17 --
git -C ~/projects/opencode-patched commit -m "Reconcile main onto the v1.17.7 patch line"
git -C ~/projects/opencode-patched diff --exit-code HEAD release/v1.17
```

2. Gate 2 currently says "with at least one plugin loaded", which is insufficient. A passive plugin that never calls `input.client.*` would not exercise `87c33b3` and could false-pass. Exact fix: require the minimal upstream-style plugin or a real plugin proven to issue an SDK client request through `input.client` while the listener is active, and record a side effect proving that client request completed. Keep the existing pass criteria of >=11 clean Question submissions and zero `reply for unknown request` / `reject for unknown request` warnings.

NON-BLOCKING SUGGESTIONS:

1. Change the local repro build command in Phase 3.1 to use `bun run script/build.ts --single`. In `v1.17.7`, `script/build.ts` recognizes `--single`; it does not recognize `--all`, and without `--single` it builds all targets by default.

2. Do not pipe patch-owned test output through `tail -30`. Let the full test output be captured so the failure cause is not hidden.

3. Make the `/tmp/opencode/v1177-verify` and `/tmp/opencode/v1177-apply` worktree setup idempotent, or use unique scratch paths. Otherwise a previous partial run can fail Phase 0 before any meaningful verification happens.

4. Tighten the `gh run watch` command in Phase 4.3. `gh run list --limit 1` can race with unrelated runs; filter by workflow, branch/ref, event, and creation time, or manually inspect the run returned before watching.

5. The build workflow release notes and failure-notice patch list are stale at nine patches, but the plan already includes Task 1.3 to fix them. Treat that task as required before publishing, not cosmetic-only, because the release body is user-visible provenance for the cutover.
