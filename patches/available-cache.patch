diff --git a/packages/core/src/catalog.ts b/packages/core/src/catalog.ts
index 1945024..4d68e64 100644
--- a/packages/core/src/catalog.ts
+++ b/packages/core/src/catalog.ts
@@ -1,7 +1,7 @@
 export * as Catalog from "./catalog"
 
 import { makeLocationNode } from "./effect/app-node"
-import { Array, Context, Effect, Layer, Option, Order, pipe, Schema } from "effect"
+import { Array, Context, Duration, Effect, Layer, Option, Order, pipe, Schema, Stream } from "effect"
 import { Catalog } from "@opencode-ai/schema/catalog"
 import { ModelV2 } from "./model"
 import { ProviderV2 } from "./provider"
@@ -102,6 +102,10 @@ const layer = Layer.effect(
       delete item.request.body.baseURL
     }
 
+    // Assigned after the caches exist below; finalize() runs only on rebuilds,
+    // which happen after layer construction completes.
+    let invalidateAvailableHook: Effect.Effect<void> = Effect.void
+
     const state = State.create<Data, Draft>({
       initial: () => ({ providers: new Map() }),
       draft: (draft) => {
@@ -165,9 +169,54 @@ const layer = Layer.effect(
             }
           }
         }
+        yield* Effect.suspend(() => invalidateAvailableHook)
         yield* events.publish(Event.Updated, {})
       }),
     })
+
+    // workstation-g3iy: /api/provider + /api/model recompute these projections
+    // on every TUI bootstrap fetch. After a serve restart, an SSE reconnect
+    // herd (every attached TUI refetching, with retry amplification as the
+    // serve slows) used to recompute the full ~5k-model projection (~300ms of
+    // synchronous JS each) hundreds of times, wedging the event loop for
+    // minutes. Collapse bursts onto a cached value: every catalog rebuild
+    // (finalize) and integration connection change invalidates it, with a TTL
+    // backstop for out-of-band changes (e.g. process.env mutation).
+    const allProviders = () => Array.fromIterable(state.get().providers.values()).map((record) => record.provider)
+    const allModels = () =>
+      pipe(
+        Array.fromIterable(state.get().providers.values()),
+        Array.flatMap((record) => {
+          return Array.fromIterable(record.models.values()).map((model) => projectModel(model, record.provider))
+        }),
+        Array.sortWith((item) => item.time.released, Order.flip(Order.Number)),
+      )
+    const computeProviderAvailable = Effect.gen(function* () {
+      const active = new Map((yield* integrations.list()).map((integration) => [integration.id, integration]))
+      return allProviders().filter((provider) =>
+        available(provider, active.get(provider.integrationID ?? Integration.ID.make(provider.id))),
+      )
+    })
+    const [providerAvailableCached, invalidateProviderAvailable] = yield* Effect.cachedInvalidateWithTTL(
+      computeProviderAvailable,
+      AVAILABLE_CACHE_TTL,
+    )
+    const computeModelAvailable = Effect.gen(function* () {
+      const providers = new Set((yield* providerAvailableCached).map((provider) => provider.id))
+      return allModels().filter((model) => providers.has(model.providerID) && model.enabled)
+    })
+    const [modelAvailableCached, invalidateModelAvailable] = yield* Effect.cachedInvalidateWithTTL(
+      computeModelAvailable,
+      AVAILABLE_CACHE_TTL,
+    )
+    const invalidateAvailable = Effect.all([invalidateProviderAvailable, invalidateModelAvailable]).pipe(Effect.asVoid)
+    invalidateAvailableHook = invalidateAvailable
+
+    yield* events.subscribe(Integration.Event.Updated).pipe(
+      Stream.runForEach(() => invalidateAvailable),
+      Effect.forkScoped({ startImmediately: true }),
+    )
+
     const result: Interface = {
       transform: state.transform,
       reload: state.reload,
@@ -182,10 +231,7 @@ const layer = Layer.effect(
         }),
 
         available: Effect.fn("CatalogV2.provider.available")(function* () {
-          const active = new Map((yield* integrations.list()).map((integration) => [integration.id, integration]))
-          return (yield* result.provider.all()).filter((provider) =>
-            available(provider, active.get(provider.integrationID ?? Integration.ID.make(provider.id))),
-          )
+          return yield* providerAvailableCached
         }),
       },
 
@@ -208,8 +254,7 @@ const layer = Layer.effect(
         }),
 
         available: Effect.fn("CatalogV2.model.available")(function* () {
-          const providers = new Set((yield* result.provider.available()).map((provider) => provider.id))
-          return (yield* result.model.all()).filter((model) => providers.has(model.providerID) && model.enabled)
+          return yield* modelAvailableCached
         }),
 
         default: Effect.fn("CatalogV2.model.default")(function* () {
@@ -293,6 +338,11 @@ const layer = Layer.effect(
 
 const SMALL_MODEL_RE = /\b(nano|flash|lite|mini|haiku|small|fast)\b/
 
+// TTL backstop for the cached provider/model availability projections; real
+// invalidation happens on every catalog rebuild and integration connection
+// change (workstation-g3iy).
+const AVAILABLE_CACHE_TTL = Duration.seconds(30)
+
 export const locationLayer = layer.pipe(
   Layer.provideMerge(Integration.locationLayer),
   Layer.provideMerge(Policy.locationLayer),
diff --git a/packages/core/test/catalog.test.ts b/packages/core/test/catalog.test.ts
index 6c736cd..47ee3db 100644
--- a/packages/core/test/catalog.test.ts
+++ b/packages/core/test/catalog.test.ts
@@ -73,6 +73,51 @@ describe("CatalogV2", () => {
     }).pipe(Effect.provide(localCatalogLayer))
   })
 
+  it.effect("collapses availability bursts onto one credential read until the catalog changes", () => {
+    const counts = { all: 0 }
+    const localCatalogLayer = Layer.fresh(
+      AppNodeBuilder.build(LayerNode.group([Catalog.node, EventV2.node, Integration.node, Policy.node]), [
+        [Location.node, locationLayer],
+        [
+          Credential.node,
+          Layer.mock(Credential.Service, {
+            all: () =>
+              Effect.sync(() => {
+                counts.all++
+                return []
+              }),
+            list: () => Effect.succeed([]),
+          }),
+        ],
+      ]),
+    )
+
+    return Effect.gen(function* () {
+      const catalog = yield* Catalog.Service
+      yield* catalog.transform((editor) => editor.provider.update(ProviderV2.ID.make("test"), () => {}))
+
+      counts.all = 0
+      // A bootstrap herd: many provider/model availability reads in a burst.
+      // Pre-cache this cost one credentials read per integration per call and
+      // wedged the serve under TUI reconnect herds (workstation-g3iy).
+      for (let i = 0; i < 25; i++) {
+        yield* catalog.provider.available()
+        yield* catalog.model.available()
+      }
+      expect(counts.all).toBeLessThanOrEqual(2)
+
+      // A catalog rebuild invalidates the cache: the next burst recomputes once.
+      yield* catalog.transform((editor) => editor.provider.update(ProviderV2.ID.make("other"), () => {}))
+      const after = counts.all
+      for (let i = 0; i < 25; i++) {
+        yield* catalog.provider.available()
+        yield* catalog.model.available()
+      }
+      expect(counts.all).toBeGreaterThan(after)
+      expect(counts.all).toBeLessThanOrEqual(after + 2)
+    }).pipe(Effect.provide(localCatalogLayer))
+  })
+
   it.effect("derives availability from a provider's integration", () => {
     const integrationID = Integration.ID.make("gateway")
     const providerID = ProviderV2.ID.make("remote")
